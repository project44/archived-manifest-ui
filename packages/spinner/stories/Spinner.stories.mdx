import { Meta, Props, Sources, Story } from '../../../.storybook/components';
import { Box } from '@manifest-ui/layout';
import { Spinner } from '../src';

<Meta component={Spinner} title="Components/Spinner" />

# Spinner

## Usage

<Story name="default" parameters={{ docs: { source: { code: '<Spinner />' } } }}>
  <Box sx={{ pb: 3 }}>
    <Spinner />
  </Box>
</Story>

## API

<Sources github="spinner" npm="@manifest-ui/spinner" />

```tsx
import { Spinner } from '@manifest-ui/react';
```

<Props />
