import { Meta, Props, Sources, Story } from '../../../.storybook/components';
import { Box } from '@manifest-ui/layout';
import { Pagination } from '../src';

<Meta component={Pagination} title="Components/Pagination" />

# Pagination

## Usage

<Story
  name="default"
  parameters={{ docs: { source: { code: '<Pagination totalRowCount={100} />' } } }}
>
  <Box sx={{ pb: 3 }}>
    <Pagination totalRowCount={100} />
  </Box>
</Story>

## API

<Sources github="pagination" npm="@manifest-ui/pagination" />

```tsx
import { Pagination } from '@manifest-ui/react';
```

<Props />
