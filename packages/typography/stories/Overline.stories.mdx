import { Box, Flex } from '@manifest-ui/layout';
import { Meta, Props, Sources, Story } from '../../../.storybook/components';
import { Overline } from '../src';

<Meta component={Overline} title="Components/Overline" />

# Overline

## Usage

<Story name="default" parameters={{ docs: { source: { code: '<Overline>Overline</Overline>' } } }}>
  <Box sx={{ pb: 3 }}>
    <Overline>Overline</Overline>
  </Box>
</Story>

### Variants

The overline comes in three variants:

<Story
  name="variants"
  parameters={{
    docs: {
      source: {
        code: `
<Overline variant="overline1">Overline</Overline>
<Overline variant="overline2">Overline</Overline>
<Overline variant="overline3">Overline</Overline>
        `,
      },
    },
  }}
>
  <Flex direction="column" gap={2} sx={{ pb: 3 }}>
    <Overline variant="overline1">Overline</Overline>
    <Overline variant="overline2">Overline</Overline>
    <Overline variant="overline3">Overline</Overline>
  </Flex>
</Story>

## API

<Sources github="typography" npm="@manifest-ui/typography" />

```tsx
import { Overline } from '@manifest-ui/react';
```

<Props />
