import { Meta, Props, Sources, Story } from '../../../.storybook/components';
import { Box } from '@manifest-ui/layout';
import { Toggle } from '../src';

<Meta component={Toggle} title="Components/Toggle" />

# Toggle

## Usage

<Story name="default" parameters={{ docs: { source: { code: '<Toggle>Toggle</Toggle>' } } }}>
  <Box sx={{ pb: 3 }}>
    <Toggle>Toggle</Toggle>
  </Box>
</Story>

### Disabled

The toggle can be disabled using the isDisabled prop:

<Story
  name="disabled"
  parameters={{ docs: { source: { code: '<Toggle isDisabled>Toggle</Toggle>' } } }}
>
  <Box sx={{ pb: 3 }}>
    <Toggle isDisabled>Toggle</Toggle>
  </Box>
</Story>

## API

<Sources github="toggle" npm="@manifest-ui/toggle" />

```tsx
import { Toggle } from '@manifest-ui/react';
```

<Props />
