import React from 'react';
import { CloseCircleOutline } from '@manifest-ui/icons';

import { Icon } from '../Icon';
import type { IconProps } from '../Icon';

export const CloseCircleOutlineIcon = (props: IconProps) => {
  return <Icon as={CloseCircleOutline} {...props} />;
};
