import { Meta, Props, Sources, Story } from '../../../.storybook/components';
import { Box } from '@manifest-ui/layout';
import { VisuallyHidden } from '../src';

<Meta component={VisuallyHidden} title="Components/VisuallyHidden" />

# VisuallyHidden

## Usage

<Story
  name="default"
  parameters={{ docs: { source: { code: '<VisuallyHidden>...</VisuallyHidden>' } } }}
>
  <Box sx={{ pb: 3 }}>
    <VisuallyHidden>Visually Hidden Text</VisuallyHidden>
  </Box>
</Story>

## API

<Sources github="visually-hidden" npm="@manifest-ui/visually-hidden" />

```tsx
import { VisuallyHidden } from '@manifest-ui/react';
```

<Props />
